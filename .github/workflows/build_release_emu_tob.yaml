name: Build MacroQuest Emu TOB Release

on:
  push:
    tags:
      - rel-emu-tob

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  call_build:
    uses: ./.github/workflows/build_release_shared.yaml
    with:
      client_target: emu-tob
    secrets: inherit