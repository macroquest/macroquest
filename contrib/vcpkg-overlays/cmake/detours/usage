detours provides CMake targets:

    find_package(detours CONFIG REQUIRED)
    target_link_libraries(main PRIVATE detours::detours)

Legacy usage (not recommended):

    find_path(DETOURS_INCLUDE_DIRS "detours/detours.h")
    find_library(DETOURS_LIBRARY detours REQUIRED)

    target_include_directories(main PRIVATE ${DETOURS_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${DETOURS_LIBRARY})
